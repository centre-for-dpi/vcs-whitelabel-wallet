diff --git a/node_modules/@animo-id/expo-digital-credentials-api/android/src/main/java/id/animo/digitalcredentials/DigitalCredentialsApiSingleton.kt b/node_modules/@animo-id/expo-digital-credentials-api/android/src/main/java/id/animo/digitalcredentials/DigitalCredentialsApiSingleton.kt
index ab45273..d7b5384 100644
--- a/node_modules/@animo-id/expo-digital-credentials-api/android/src/main/java/id/animo/digitalcredentials/DigitalCredentialsApiSingleton.kt
+++ b/node_modules/@animo-id/expo-digital-credentials-api/android/src/main/java/id/animo/digitalcredentials/DigitalCredentialsApiSingleton.kt
@@ -68,19 +68,22 @@ object DigitalCredentialsApiSingleton : SingletonModule {
         val registryManager = RegistryManager.create(context)
         val matcherInstance = loadMatcher(context, matcher)
 
-        // For backward compatibility with Chrome
-        registryManager.registerCredentials(
-                request =
-                        object :
-                                RegisterCredentialsRequest(
-                                        "com.credman.IdentityCredential",
-                                        "openid4vp",
-                                        credentialBytes,
-                                        matcherInstance
-                                ) {}
-        )
-
-        // In the future, should only register this type
+        // PATCHED (C.7.3b troubleshooting): the package used to register
+        // TWICE with the same id ("openid4vp") — once under the legacy type
+        // string "com.credman.IdentityCredential" ("for backward
+        // compatibility with Chrome"), then again under the real system type
+        // DigitalCredential.TYPE_DIGITAL_CREDENTIAL (confirmed byte-for-byte
+        // identical to what androidx.credentials.registry.provider.
+        // digitalcredentials.DigitalCredentialRegistry hardcodes internally —
+        // decompiled from registry-provider's own .aar). CMWallet, the
+        // reference wallet that DOES successfully appear in Chrome's picker
+        // on this exact device/OS, registers ONLY under that one standard
+        // type, via DigitalCredentialRegistry, never the legacy string.
+        // Suspected cause: two RegisterCredentialsRequest calls sharing the
+        // same id under different types either raced or left the system
+        // with an inconsistent/overwritten registration, even though both
+        // calls individually resolved without throwing. Dropping the legacy
+        // call to match CMWallet's known-working shape.
         registryManager.registerCredentials(
                 request =
                         object :
diff --git a/node_modules/@animo-id/expo-digital-credentials-api/build/encodeCredentials.js b/node_modules/@animo-id/expo-digital-credentials-api/build/encodeCredentials.js
index 6b22ef3..0a52a3f 100644
--- a/node_modules/@animo-id/expo-digital-credentials-api/build/encodeCredentials.js
+++ b/node_modules/@animo-id/expo-digital-credentials-api/build/encodeCredentials.js
@@ -104,6 +104,18 @@ export function getEncodedCredentialsBase64(items, { debug }) {
             mso_mdoc: mdocCredentials,
             'dc+sd-jwt': sdJwtCredentials,
         },
+        // PATCHED (see patches/@animo-id+expo-digital-credentials-api+0.4.0.patch):
+        // the package never set this field, so #[nserde(default)] on the
+        // matcher's Rust side (CMWallet/matcher-rs/src/openid4vp_models.rs)
+        // deserializes it as an empty Vec — the matcher's outer loop
+        // (`for target_protocol in &registry.supported_protocols`) then never
+        // iterates, so no request is ever matched regardless of doctype or
+        // claims. Values must equal pr.protocol literally, per the matcher's
+        // own explicit allowlist (CMWallet/matcher-rs/src/openid4vp.rs) — NOT
+        // the bare "openid4vp" string DigitalCredentialsApiSingleton.kt uses
+        // when registering (that string is the Android
+        // RegisterCredentialsRequest's own protocol id, a separate concept).
+        supported_protocols: ['openid4vp-v1-unsigned', 'openid4vp-v1-signed', 'openid4vp-v1-multisigned'],
     };
     // Convert JSON to bytes and add to chunks
     chunks.push(textEncoder.encode(JSON.stringify(registryJson)));
